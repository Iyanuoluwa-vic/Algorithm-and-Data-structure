package Array;

public class SinglyLinkedList {
    int arr[]=null;


    public SinglyLinkedList(int size){// constructor than create an array
        arr =new int [size];
        for(int i=0; i <arr.length;i++){
               arr[i]=Integer.MIN_VALUE;    //Hold lowest value integer can have
        }
    }


    public static void 
}
